import { TaskImportancePipe } from './task-importance.pipe';

describe('TaskImportancePipe', () => {
  it('create an instance', () => {
    const pipe = new TaskImportancePipe();
    expect(pipe).toBeTruthy();
  });
});
